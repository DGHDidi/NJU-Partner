# 南大轻搭子系统配置与使用说明

本文档面向第一次拿到项目的使用者，说明如何从零环境完成安装、配置、启动和基础测试。文档中的数据库账号、密码、主机等均使用占位符，请根据自己的环境填写，不要直接提交真实生产密码。

## 1. 环境准备

需要提前安装：

- JDK 17
- Maven 3.9 或以上
- Node.js 18 或以上
- MySQL 8.x
- Git

建议确认版本：

```bash
java -version
mvn -version
node -v
npm -v
mysql --version
git --version
```

项目默认端口：

- 后端服务：`http://localhost:8080`
- 前端服务：`http://localhost:5173`
- 前端 API 代理：`/api` 转发到 `http://localhost:8080`
- 默认数据库名：`nju_partner`

## 2. 获取项目

```bash
git clone <repository-url>
cd NJU-Partner
```

项目主要目录：

```text
NJU-Partner
├── backend/nju-partner-backend      # Spring Boot 后端
├── frontend/nju-partner-frontend    # Vue 前端
├── sql/init.sql                     # 数据库初始化脚本
├── README.md
├── 接口文档.md
└── 系统配置与使用说明.md
```

## 3. 配置 MySQL

### 3.1 创建数据库和项目用户

可以使用已有 MySQL 用户，也可以新建一个专用于本项目的用户。以下命令需要用有建库和授权权限的 MySQL 管理员账号执行，示例中的 `<db_user>` 和 `<db_password>` 请替换为你自己的值。

```sql
CREATE DATABASE IF NOT EXISTS nju_partner
    DEFAULT CHARACTER SET utf8mb4
    DEFAULT COLLATE utf8mb4_unicode_ci;
CREATE USER '<db_user>'@'localhost' IDENTIFIED BY '<db_password>';
GRANT ALL PRIVILEGES ON nju_partner.* TO '<db_user>'@'localhost';
FLUSH PRIVILEGES;
```

如果 MySQL 不在本机，请把 `localhost` 替换为实际访问来源或按你的 MySQL 安全策略配置。

### 3.2 初始化数据库

项目提供统一初始化脚本：

```text
sql/init.sql
```

该脚本会创建数据库和全部表结构，并写入基础分类数据和一个管理员种子账号。重新执行脚本会删除并重建已有表，已有测试数据会被清空。

执行方式。这里可以使用上一步创建的项目用户；如果你的 MySQL 权限策略更严格，也可以使用有权限执行建表和删表的初始化账号。

```bash
mysql --default-character-set=utf8mb4 -u<db_user> -p < sql/init.sql
```

如果你的终端不支持上述重定向方式，也可以进入 MySQL 后执行：

```sql
SOURCE /absolute/path/to/NJU-Partner/sql/init.sql;
```

Windows 环境可使用正斜杠路径，例如：

```sql
SOURCE D:/projects/NJU-Partner/sql/init.sql;
```

## 4. 配置后端

后端配置文件位于：

```text
backend/nju-partner-backend/src/main/resources/application.yml
```

至少需要确认数据库连接信息：

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/nju_partner?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: <db_user>
    password: <db_password>

jwt:
  secret: <change-this-secret-in-your-env>
  expiration: 86400000
```

说明：

- `username` 和 `password` 填写你自己的 MySQL 账号。
- `jwt.secret` 建议在正式部署前改成足够长的随机字符串。
- 如果 MySQL 端口不是 `3306`，需要同步修改 JDBC URL。
- 如果后端端口 `8080` 被占用，可修改 `server.port`。

## 5. 启动后端

进入后端目录：

```bash
cd backend/nju-partner-backend
```

安装依赖并启动：

```bash
mvn spring-boot:run
```

看到类似日志说明后端启动成功：

```text
Tomcat started on port 8080
```

也可以打包后运行：

```bash
mvn package -DskipTests
java -jar target/nju-partner-backend-1.0.0.jar
```

检查后端是否可访问：

```bash
curl "http://localhost:8080/api/posts?pageNum=1&pageSize=10"
```

Windows PowerShell 也可以使用：

```powershell
Invoke-WebRequest -UseBasicParsing "http://localhost:8080/api/posts?pageNum=1&pageSize=10"
```

## 6. 配置并启动前端

进入前端目录：

```bash
cd frontend/nju-partner-frontend
```

安装依赖：

```bash
npm install
```

启动开发服务器：

```bash
npm run dev
```

浏览器访问：

```text
http://localhost:5173/
```

前端开发环境通过 Vite 代理访问后端。代理配置位于：

```text
frontend/nju-partner-frontend/vite.config.js
```

默认配置：

```js
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
    },
  },
}
```

如果后端部署在其他地址，需要修改 `target`。

## 7. 基本使用流程

建议至少准备两个普通用户和一个管理员账号进行测试。

1. 使用普通用户 A 注册并登录。
2. 用户 A 完善个人资料，发布一个组队帖。
3. 使用普通用户 B 注册并登录。
4. 用户 B 在首页筛选帖子，进入帖子详情页，提交报名申请。
5. 用户 A 回到帖子详情页，在报名列表中通过或拒绝用户 B 的申请。
6. 用户 B 查看页眉通知，确认审核结果。
7. 用户 A 和用户 B 在评论区发表评论和回复，检查评论回复通知。
8. 用户 A 查看已报名成员列表，确认楼主和已通过成员都正确显示。
9. 使用管理员账号登录后台，检查帖子、用户、评论管理功能。

## 8. 常用命令

后端测试：

```bash
cd backend/nju-partner-backend
mvn test
```

前端生产构建：

```bash
cd frontend/nju-partner-frontend
npm run build
```

前端本地预览生产构建：

```bash
npm run preview
```

检查端口占用：

```bash
netstat -ano | findstr :8080
netstat -ano | findstr :5173
```

## 9. 常见问题

### 浏览器提示 ERR_CONNECTION_REFUSED

说明对应端口没有服务在运行。请确认：

- 前端是否已经执行 `npm run dev`。
- 后端是否已经执行 `mvn spring-boot:run`。
- 浏览器访问的是前端地址 `http://localhost:5173/`。
- 后端接口地址 `http://localhost:8080/api/posts` 是否能访问。

### 前端页面能打开，但接口失败

请确认：

- 后端服务已启动。
- `vite.config.js` 中 `/api` 代理目标和后端端口一致。
- 后端 `application.yml` 数据库配置正确。
- MySQL 服务已启动，且 `sql/init.sql` 已成功执行。

### 登录或注册失败

请确认：

- 数据库连接正常。
- 用户名没有重复。
- 注册字段符合后端校验要求。
- 如果使用管理员种子账号，请确认初始化脚本中的账号数据没有被修改。

### 修改代码后没有生效

- 修改前端 Vue/CSS 后，Vite 通常会自动热更新；异常时刷新浏览器。
- 修改后端 Java 或配置文件后，需要重启后端服务。
- 修改数据库结构后，需要重新执行初始化脚本或编写迁移脚本。

## 10. 生产部署提醒

第一版项目主要面向课程展示和本地/局域网部署。正式上线前建议额外完成：

- 删除或修改初始化脚本中的默认管理员账号。
- 使用环境变量或外部配置管理数据库密码和 JWT 密钥。
- 配置 HTTPS。
- 前端执行 `npm run build` 后部署 `dist` 静态文件。
- 后端打包为 jar 后由进程管理工具托管。
- 为数据库配置独立低权限账号。
- 增加日志轮转、备份和监控。
