# 南大轻搭子系统配置与使用说明

本文档说明如何从零配置、启动和测试本项目。

## 1. 环境要求

- JDK 17
- Maven 3.9+
- Node.js 18+
- MySQL 8.x

项目默认端口：

- 后端：`http://localhost:8080`
- 前端：`http://localhost:5173`
- 数据库：`nju_partner`

## 2. 数据库初始化

本项目只保留一个数据库初始化脚本：

```text
sql/init.sql
```

该脚本已经包含完整表结构，包括用户、帖子、报名、评论回复、站内通知、收藏、分类等表。不要再单独执行旧的迁移脚本。

使用 MySQL 初始化数据库：

```powershell
mysql --default-character-set=utf8mb4 -uroot -p1234 -e "source C:/Users/DGH_Didi/Documents/NJU-Partner/sql/init.sql"
```

如果你的 MySQL 密码不是 `1234`，需要替换命令中的密码，并同步修改后端配置文件：

```text
backend/nju-partner-backend/src/main/resources/application.yml
```

默认数据库配置：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nju_partner?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 1234
```

## 3. 启动后端

进入后端目录：

```powershell
cd C:\Users\DGH_Didi\Documents\NJU-Partner\backend\nju-partner-backend
```

开发方式启动：

```powershell
mvn.cmd spring-boot:run
```

看到类似下面日志表示后端启动成功：

```text
Tomcat started on port 8080
```

也可以先打包再运行：

```powershell
mvn.cmd package -DskipTests
java -jar target\nju-partner-backend-1.0.0.jar
```

## 4. 启动前端

进入前端目录：

```powershell
cd C:\Users\DGH_Didi\Documents\NJU-Partner\frontend\nju-partner-frontend
```

首次运行先安装依赖：

```powershell
npm install
```

启动开发服务器：

```powershell
npm run dev
```

浏览器访问：

```text
http://localhost:5173/
```

前端会通过 Vite 代理把 `/api` 请求转发到 `http://localhost:8080`。

## 5. 默认账号

初始化脚本会创建管理员账号：

```text
username: admin
password: 123456
```

普通用户可以在注册页自行注册。

## 6. 功能测试建议

建议至少准备两个普通用户和一个管理员：

1. 用户 A 注册登录，发布组队帖子。
2. 用户 B 注册登录，在帖子详情页报名。
3. 用户 A 在帖子详情页审核报名，通过或拒绝。
4. 用户 B 查看页眉通知，确认审核状态通知是否出现。
5. 用户 B 在评论区回复用户 A 的评论。
6. 用户 A 查看页眉通知，确认评论回复通知是否出现。
7. 帖子详情页检查“已报名成员”是否显示楼主和已通过报名成员。

## 7. 常用验证命令

后端编译测试：

```powershell
cd C:\Users\DGH_Didi\Documents\NJU-Partner\backend\nju-partner-backend
mvn.cmd test
```

前端生产构建：

```powershell
cd C:\Users\DGH_Didi\Documents\NJU-Partner\frontend\nju-partner-frontend
npm run build
```

检查后端接口：

```powershell
Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:8080/api/posts?pageNum=1&pageSize=10"
```

检查端口占用：

```powershell
netstat -ano | findstr :8080
netstat -ano | findstr :5173
```

## 8. 注意事项

- 重新执行 `sql/init.sql` 会删除并重建数据库表，已有测试数据会被清空。
- 修改后端 Java 代码后需要重启后端。
- 修改前端 Vue 代码后，Vite 通常会自动热更新；如果页面异常，刷新浏览器即可。
- 如果浏览器提示 `ERR_CONNECTION_REFUSED`，优先检查对应端口服务是否已经启动。
