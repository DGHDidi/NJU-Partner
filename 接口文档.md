# 南大轻搭子接口文档

本文档描述 NJU Partner 第一版后端接口。接口默认以 `/api` 开头，开发环境中前端通过 Vite 代理把 `/api` 请求转发到后端服务。

## 1. 通用约定

### 1.1 基础地址

开发环境：

```text
http://localhost:8080
```

前端项目中通常直接请求：

```text
/api/...
```

### 1.2 认证方式

除注册、登录、帖子公开列表、帖子详情、评论列表等公开接口外，受保护接口需要携带 JWT：

```http
Authorization: Bearer <token>
```

`token` 由登录接口返回。

### 1.3 统一返回格式

后端接口统一返回 `Result<T>`：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

常见状态码：

| code | 含义 |
| --- | --- |
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或 token 无效 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

### 1.4 分页格式

分页接口通常接收：

| 参数 | 说明 |
| --- | --- |
| `pageNum` | 页码，默认 `1` |
| `pageSize` | 每页数量，默认 `10` |

分页返回数据通常包含：

```json
{
  "records": [],
  "total": 0,
  "size": 10,
  "current": 1,
  "pages": 0
}
```

### 1.5 时间格式

后端支持常见本地时间格式：

```text
yyyy-MM-dd HH:mm:ss
```

也兼容 ISO 本地时间：

```text
2026-06-08T14:30:00
```

## 2. 数据对象

### 2.1 用户注册 UserRegisterDTO

```json
{
  "username": "alice",
  "password": "password1",
  "nickname": "Alice",
  "campus": "仙林校区",
  "grade": "大一",
  "major": "计算机科学与技术"
}
```

字段说明：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `username` | 是 | 用户名，1 到 10 个字符 |
| `password` | 是 | 密码，6 到 100 个字符 |
| `nickname` | 是 | 昵称 |
| `campus` | 是 | 校区 |
| `grade` | 是 | 年级 |
| `major` | 是 | 专业 |

### 2.2 用户登录 UserLoginDTO

```json
{
  "username": "alice",
  "password": "password1"
}
```

### 2.3 发帖 PostCreateDTO

```json
{
  "title": "今晚羽毛球缺一人",
  "type": "运动搭子",
  "description": "新手友好，主要活动一下。",
  "location": "方肇周体育馆",
  "activityTime": "2026-06-08 19:30:00",
  "needCount": 1,
  "campus": "仙林校区",
  "gradeLimit": "不限",
  "majorLimit": "不限",
  "contact": "微信：example"
}
```

字段说明：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `title` | 是 | 标题，不能为空白字符 |
| `type` | 是 | 活动类型 |
| `description` | 是 | 活动描述 |
| `location` | 否 | 具体地点 |
| `activityTime` | 是 | 活动时间，不能早于当前时间 |
| `needCount` | 是 | 除楼主外还需要的人数，最小为 1 |
| `campus` | 是 | 活动校区 |
| `gradeLimit` | 否 | 年级限制 |
| `majorLimit` | 否 | 专业限制 |
| `contact` | 是 | 联系方式 |

### 2.4 帖子查询 PostQueryDTO

| 参数 | 说明 |
| --- | --- |
| `campus` | 按校区筛选 |
| `type` | 按活动类型筛选 |
| `keyword` | 按关键词搜索 |
| `status` | 按帖子状态筛选 |
| `grade` | 按年级筛选 |
| `startTime` | 活动开始时间 |
| `endTime` | 活动结束时间 |
| `userId` | 按发布者筛选 |
| `pageNum` | 页码 |
| `pageSize` | 每页数量 |

帖子状态：

| status | 含义 |
| --- | --- |
| 0 | 招募中 |
| 1 | 已成团 |
| 2 | 已关闭 |
| 3 | 已过期 |

### 2.5 报名 ApplicationCreateDTO

```json
{
  "message": "我想报名，可以准时到。"
}
```

报名状态：

| status | 含义 |
| --- | --- |
| 0 | 待审核 |
| 1 | 已通过 |
| 2 | 已拒绝 |
| 3 | 已取消 |

### 2.6 评论 CommentCreateDTO

```json
{
  "content": "我也想了解一下具体时间。",
  "parentId": 1
}
```

`parentId` 可为空；为空表示一级评论，不为空表示回复某条评论。

## 3. 用户接口

### 注册

```http
POST /api/user/register
Content-Type: application/json
```

请求体：`UserRegisterDTO`

成功返回：

```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

### 登录

```http
POST /api/user/login
Content-Type: application/json
```

请求体：`UserLoginDTO`

成功返回：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "<token>",
    "userInfo": {
      "id": 1,
      "username": "alice",
      "nickname": "Alice",
      "campus": "仙林校区",
      "grade": "大一",
      "major": "计算机科学与技术",
      "role": "USER",
      "status": 1
    }
  }
}
```

### 获取当前用户资料

```http
GET /api/user/profile
Authorization: Bearer <token>
```

### 修改当前用户资料

```http
PUT /api/user/profile
Authorization: Bearer <token>
Content-Type: application/json
```

请求体：

```json
{
  "nickname": "新昵称",
  "avatar": "",
  "campus": "鼓楼校区",
  "grade": "大二",
  "major": "软件工程"
}
```

### 我的发布

```http
GET /api/user/my-posts?pageNum=1&pageSize=10
Authorization: Bearer <token>
```

### 我的报名

```http
GET /api/user/my-applications?pageNum=1&pageSize=10
Authorization: Bearer <token>
```

### 我的收藏

```http
GET /api/user/my-favorites?pageNum=1&pageSize=10
Authorization: Bearer <token>
```

兼容接口：

```http
GET /api/user/favorites?pageNum=1&pageSize=10
```

## 4. 帖子接口

### 创建帖子

```http
POST /api/posts
Authorization: Bearer <token>
Content-Type: application/json
```

请求体：`PostCreateDTO`

### 查询帖子列表

```http
GET /api/posts?keyword=羽毛球&campus=仙林校区&type=运动搭子&pageNum=1&pageSize=10
```

返回：`IPage<PostVO>`

### 查看帖子详情

```http
GET /api/posts/{id}
```

### 更新帖子

```http
PUT /api/posts/{id}
Authorization: Bearer <token>
Content-Type: application/json
```

请求体：`PostCreateDTO`

### 关闭帖子

```http
PUT /api/posts/{id}/close
Authorization: Bearer <token>
```

### 删除帖子

```http
DELETE /api/posts/{id}
Authorization: Bearer <token>
```

## 5. 报名接口

### 报名帖子

```http
POST /api/posts/{id}/apply
Authorization: Bearer <token>
Content-Type: application/json
```

请求体：`ApplicationCreateDTO`

### 查看某帖报名列表

```http
GET /api/posts/{id}/applications
Authorization: Bearer <token>
```

通常仅楼主或管理员可查看完整报名列表。

### 查看我在某帖的报名状态

```http
GET /api/posts/{id}/applications/me
Authorization: Bearer <token>
```

### 查看某帖已报名成员

```http
GET /api/posts/{id}/members
```

返回楼主和已通过报名成员，用于公开展示。

### 通过报名

```http
PUT /api/applications/{id}/pass
Authorization: Bearer <token>
```

### 拒绝报名

```http
PUT /api/applications/{id}/reject
Authorization: Bearer <token>
```

### 取消报名

```http
PUT /api/applications/{id}/cancel
Authorization: Bearer <token>
```

## 6. 评论接口

### 创建评论或回复

```http
POST /api/posts/{id}/comments
Authorization: Bearer <token>
Content-Type: application/json
```

请求体：

```json
{
  "content": "评论内容",
  "parentId": null
}
```

回复评论时传入 `parentId`。

### 获取帖子评论

```http
GET /api/posts/{id}/comments
```

### 删除评论

```http
DELETE /api/comments/{id}
Authorization: Bearer <token>
```

## 7. 收藏接口

### 添加收藏

```http
POST /api/posts/{id}/favorite
Authorization: Bearer <token>
```

### 取消收藏

```http
DELETE /api/posts/{id}/favorite
Authorization: Bearer <token>
```

## 8. 通知接口

### 获取我的通知

```http
GET /api/notifications
Authorization: Bearer <token>
```

### 获取未读数量

```http
GET /api/notifications/unread-count
Authorization: Bearer <token>
```

返回：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "count": 3
  }
}
```

### 标记单条通知已读

```http
PUT /api/notifications/{id}/read
Authorization: Bearer <token>
```

### 全部标记已读

```http
PUT /api/notifications/read-all
Authorization: Bearer <token>
```

## 9. 管理员接口

管理员接口均需要管理员登录态。

### 帖子管理列表

```http
GET /api/admin/posts?pageNum=1&pageSize=10
Authorization: Bearer <admin-token>
```

支持 `PostQueryDTO` 中的查询参数。

### 删除帖子

```http
DELETE /api/admin/posts/{id}
Authorization: Bearer <admin-token>
```

### 用户管理列表

```http
GET /api/admin/users?pageNum=1&pageSize=10&keyword=alice
Authorization: Bearer <admin-token>
```

### 封禁用户

```http
PUT /api/admin/users/{id}/ban
Authorization: Bearer <admin-token>
```

### 解封用户

```http
PUT /api/admin/users/{id}/unban
Authorization: Bearer <admin-token>
```

### 评论管理列表

```http
GET /api/admin/comments?pageNum=1&pageSize=10&keyword=测试
Authorization: Bearer <admin-token>
```

